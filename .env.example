# ============================================
# COOLIFY DEPLOYMENT - ENVIRONMENT VARIABLES
# ============================================
# Copy this file to .env or set these in Coolify UI
# Required variables: POSTGRES_PASSWORD, JWT_SECRET
# Optional: Domain names (if different from defaults)

# ============================================
# DOMAIN CONFIGURATION
# ============================================
# Set these in Coolify UI under "Domains"
# Default values are shown - change if using different domains

BACKEND_DOMAIN=api.strastix.com
FRONTEND_DOMAIN=strastix.com
TEST_CASINO_DOMAIN=test.strastix.com

# ============================================
# DATABASE CONFIGURATION
# ============================================
# PostgreSQL is embedded in docker-compose.yml
# Internal connection only - not exposed to internet

POSTGRES_USER=strastix_user
POSTGRES_PASSWORD=CHANGE_THIS_TO_STRONG_PASSWORD
POSTGRES_DB=strastix_db

# IMPORTANT: Avoid special characters in POSTGRES_PASSWORD
# Safe characters: A-Z, a-z, 0-9, _ (underscore)
# Avoid: @ # ! % & * ( ) = + [ ] { } | \ / ? < > , . ; : ' " `

# Note: DATABASE_URL is auto-generated in docker-compose.yml
# Format: postgresql://POSTGRES_USER:POSTGRES_PASSWORD@postgres:5432/POSTGRES_DB

# ============================================
# REDIS CONFIGURATION
# ============================================
# Redis is embedded in docker-compose.yml
# Internal connection only - not exposed to internet
# No password required (internal network only)

# Connection: redis://redis:6379
# REDIS_PASSWORD is automatically set to empty string in docker-compose.yml

# ============================================
# APPLICATION SECURITY
# ============================================
# JWT Secret - REQUIRED
# Generate strong secret: openssl rand -base64 64

JWT_SECRET=CHANGE_THIS_TO_RANDOM_STRING_MIN_32_CHARS

# ============================================
# OPTIONAL INTEGRATIONS
# ============================================
# Leave empty if not using these services

# Telegram Bot Integration
TELEGRAM_BOT_TOKEN=

# Meta Pixel Tracking
META_PIXEL_ID=
META_ACCESS_TOKEN=

# Google Ads Conversion Tracking
GOOGLE_ADS_ID=
GOOGLE_API_SECRET=

# ============================================
# COOLIFY DEPLOYMENT CHECKLIST
# ============================================
#
# 1. Set Environment Variables in Coolify UI:
#    - POSTGRES_PASSWORD (required)
#    - JWT_SECRET (required)
#    - Domain names (if different from defaults)
#    - Optional integrations (if using)
#
# 2. Configure Domains in Coolify:
#    - api.strastix.com -> Backend service
#    - strastix.com -> Frontend service
#    - test.strastix.com -> Test Casino Demo service
#
# 3. SSL/TLS:
#    - Automatic via Coolify's Traefik
#    - Let's Encrypt certificates
#
# 4. Database & Redis:
#    - Embedded in docker-compose.yml
#    - Data persists in named volumes
#    - No external access needed
#
# 5. Health Checks:
#    - All services have health checks
#    - Backend waits for DB + Redis
#    - Frontend waits for Backend
#
# ============================================
